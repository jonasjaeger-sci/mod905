MD flux simulation using C
==========================

This folder contains scripts to run the MD FLUX simulation for
the 2D WCA example in a high barrier implemented in C.

The C code must be compiled before it can be executed and this
is done by running ``python3 setup.py build_ext --inplace``.

This particular example is for python3 only.
