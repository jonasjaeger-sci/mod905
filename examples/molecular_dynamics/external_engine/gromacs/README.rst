GROMACS external engine test
============================

This is just a simple test using the GROMACS external engine.
We check that we can run forward in time, reverse the velocities,
run backward in time and generate a trajectory identical to GROMACS.

For comparison a GROMACS trajectory needs to be generated and the commands
to do that can be found in plain-md/cmds.sh.
